# VirtualOutfit AI Backend - Environment Variables
# Copy this file to .env and fill in your values

# ============================================
# Google Cloud / Vertex AI Configuration
# ============================================

# Your Google Cloud Project ID (required)
GOOGLE_CLOUD_PROJECT=your-project-id

# Google Cloud region for Vertex AI (default: us-central1)
GOOGLE_CLOUD_LOCATION=us-central1

# Option 1: Path to service account JSON file (for local development)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Option 2: JSON credentials as string (for Vercel/cloud deployment)
# Copy the entire contents of your service account JSON file here
# GOOGLE_CREDENTIALS_JSON={"type": "service_account", "project_id": "...", ...}

# ============================================
# Server Configuration
# ============================================

PORT=8000
